// The admin layout redirects `/admin` to either `/admin/overview` or
// `/admin/login` before this ever paints — this route has no content of its own.
export default function AdminIndexPage() {
  return null;
}
